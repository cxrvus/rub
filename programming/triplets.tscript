function cubicTriplets(n)
{
	var cubes = [];

	for var k in 0:n do {
		cubes.push(math.pow(k, 3));
	}

	var triplets = [];

	for var x in cubes do {
		var entry = [(x)%n, (x//n)%n, (x//math.pow(n, 2))%n];
		entry = String.join(entry, ":");
		triplets.push(entry);
	}

	return triplets;
}
