function histogram(str) {
	var words = str.toLowerCase().split(" ");
	var dict = {};

	for var word in words do {
		if word == "" then continue;
		else if not dict.has(word) then dict[word] = 1;
		else dict[word] += 1;
	}

	return dict;
}

print(histogram("Ich bin ich und ICH bin hier"));
